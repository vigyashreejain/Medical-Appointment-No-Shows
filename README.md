# Medical-Appointment-No-Shows
In this project, the raw data was cleaned and preprocessed by handling missing values, removing duplicate records, standardizing column names, and converting date formats into a consistent structure. The target variable (no_show) was also transformed into a numerical format for easier analysis
